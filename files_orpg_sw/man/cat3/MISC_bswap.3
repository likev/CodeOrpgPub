MISC_bswap(3) 		   ORPG SDD Manpage   		MISC_bswap(3)

NAME
MISC_bswap() - perform byte swap of an array of numbers.

SYNOPSIS
#include <misc.h>
int MISC_bswap (int swap_size, void *in_buf, 
		int n_items, void *out_buf);

DESCRIPTION
MISC_bswap performs byte swap of an array of "n_item" elements if the 
local machine is of little endian. The size of each element is 
"swap_size" bytes, which must be 2, 4, or 8. The output buffer 
out_buf" can overlap with or identical to the input array "in_buf".

RETURN VALUE
MISC_bswap returns the number of items processed (n_items) on success 
or -1 on failure (when one of the argument is bad).
