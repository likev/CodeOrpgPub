ntda_conf(4)           ORPG Manpage              ntda_conf(4)


NAME:
        ntda_conf.lb - NTDA Confidence Final Product

DESCRIPTION :
        The ntda_conf linear buffer contains product headers for the
        64 level/1.08 nm (2.0 km) x 1 degree resolution NTDA Confidence 
        Final Product, EDC.  EDC is output from the NEXRAD Turbulence 
        Detection Algorithm (NTDA) Final Product task (ntda_fp), which
        gets its input from the NTDA algorithm task (ntda_alg) 
        Intermediate Product NTDA_CONF_IP.

        The product indicates the confidence associated with the
        NTDA_EDR (156) final product, EDR.

PRODUCT ID:
        NTDA_CONF (157)

PRODUCT CODE:
        157

DEPENDENT ON PRODUCTS:
        COMBBASE_ELEV (303)
        RECCLDIGREF (298)
 
TYPE:
        Elevation Based

GENERATING TASK:
        ntda_fp (cpc099/tsk002)

PATH:
        kinematic/ntda_conf.lb

HDR:
        a309.h

SEE ALSO product_tables(4), ntda_conf_ip(4), ntda_alg(1), ntda_fp(1)

                             $Revision $

