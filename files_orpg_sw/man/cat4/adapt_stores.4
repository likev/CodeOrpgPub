adapt_stores(4)         Adaptation Data Stores                       adapt_stores(4)

DESCRIPTION
       The adapt_stores configuration file contains a list of meta-data driven
    adaptation configuration files (see meta_config_files.4).  Each configuration
    file contains factory default adaptation parameters.  The mnttsk_adapt(1)
    program will initialize ORPG adaptation data from the configuration files
    listed in this file.  The files will be processed in order.  If any names
    from each configuration file are the same, the data from the later file
    will be used.

SEE ALSO
misc_adapt(4), meta_config_files(4), algorithms(4), prod_params(4),
mnttsk_adapt(1), install_adapt_type(1), hci_spp(1), hci_adapt(1)

