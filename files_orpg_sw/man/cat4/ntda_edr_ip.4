ntda_edr_ip(4)               ORPG Manpage                ntda_edr_ip(4)


NAME:
        ntda_edr_ip.lb - NTDA EDR Intermediate Product

DESCRIPTION :
        The ntda_edr_ip linear buffer contains product headers for the
        NEXRAD Turbulence Detection Algorithm (NTDA) intermediate products.
        The products are output from the NTDA algorithm task (ntda_alg).

        The NTDA_EDR_IP product contains values for turbulence intensity,
        eddy dissipation rate (EDR).  Associated quality control indices, or 
        confidences, are stored in the NTDA Confidence intermediate product, 
        NTDA_CONF_IP (316).

PRODUCT ID:
        NTDA_EDR_IP (315)

PRODUCT CODE:
        0 (intermediate product)

DEPENDENT ON PRODUCTS:
        RECCLDIGREF (298)
        COMBBASE_ELEV (303)

TYPE:
        Elevation Based

GENERATING TASK:
        ntda_alg (cpc099/tsk001)

PATH:
        kinematic/ntda_edr_ip.lb

HDR:
        a309.h

SEE ALSO product_tables(4), ntda_alg(1), ntda_fp(1), ntda_edr(4),
ntda_conf_ip(4)

                             $Revision $

