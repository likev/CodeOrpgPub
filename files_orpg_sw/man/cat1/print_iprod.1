print_iprod(1)                   RPG                  print_iprod(1)

NAME
    print_iprod - Menu driven tool for printing RPG intermediate product data.

SYNOPSIS
    print_iprod

DESCRIPTION
    Create an "extensions" directory under $CFG_DIR.  Copy the following files
    to the $CFG_DIR/extensions directory: 

       - product_attr_table.print_iprod
       - task_attr_table.print_iprod

    Run the tool at the command line.  At the menu prompt, choose which
    product(s) you want to have printed.  The tool will wait for the chosen
    product to be updated, then it will print the data to the screen.

    NOTE: A lot of text is output to the screen.  You may want to script the 
    output to a file.

    To extend the tool to print more products:

    (1) update the necessary lines in the extension files
        (product_attr_table.print_iprod and task_attr_table.print_iprod)
    (2) update the print_iprod.h header file (PRINT_IPROD_NUM_PRODS)
    (3) update the "Prod_names" array defined in print_iprod.c
    (4) print_iprod.c: update the Print_prod_data() function to recognize the
        new data 
    (5) print_iprod.c: add a function (called from Print_prod_data) to print the
        new data
