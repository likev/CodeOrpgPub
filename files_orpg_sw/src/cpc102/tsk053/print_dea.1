print_dea (1) 		   	ORPG Manpage   			print_dea(1)

    
NAME
    print_dea
    
SYNOPSIS
    print_dea [options] [DE_ID]
    
DESCRIPTION

    "print_dea" is a tool that retrieves selected Data Element Attributes from
    the data element attribute database and prints them on the terminal. It can
    also be used to modify the value attribute of a specified data element.

    If "DE_ID" is a full ID of a data element, "print_dea" prints all
    attributes of the data element. For example, "print_dea site_info.rda_lat"
    prints all attributes of data element "site_info.rda_lat". If "DE_ID" is a
    node ID of the data element tree, "print_dea" prints all branch names of
    the node. For example, "print_dea site_info" prints all branch names of
    node "site_info". If "DE_ID" is missing, all branch names of the root node
    are printed.

    To change the value attribute for a data element, one can use the -n
    option. When this option is selected, "DE_ID" must be a valid data element
    ID.

OPTIONS
    "print_dea" recognizes the following options:

	-f DB_file_name		Specifies the name of the Data Element 
				Attribute database file (an LB file). The 
				default is the RPG data element database.

	-n new_value		Specifies the new value for updating the value
				attribute of data element "DE_ID". The default 
				is printing the data element attributes only 
				(not updating the value attribute).

    	-h			Show the usage information.
  
EXAMPLES

    print_dea

	- Prints all branch names of the root node.

    print_dea alg

	- Prints all branch names of node "alg".

    print_dea alg.mda

	- Prints all branch names of node "alg.mda". For algorithm adaptation
	  data, these are names of all parameters for "mda".

    print_dea alg.mda.min_filter_rank

	- Prints all attributes of data element "alg.mda.min_filter_rank".

    print_dea -n 4 alg.mda.min_filter_rank

	- Changes the value attribute of data element 
	  "alg.mda.min_filter_rank" to 4.
	
        
SEE ALSO
    deau (3), deau (4)
    
    				
