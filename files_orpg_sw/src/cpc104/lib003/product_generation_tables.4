product_generation_tables(4)    ORPG SUM Manpage 	product_generation_tables(4)

NAME
product_generation_tables - Default generation tables.

DESCRIPTION
   The product_generation_tables file is a cs(3) format file that contains a
   Default_prod_gen entry.  The Default_prod_gen entry contains a table of 
   default product generation information for each product defined for ORPG.  
   The fields for each product generation table are documented below.  Items
   enclosed in <> are sub-parameters of the main parameter that they are listed 
   next to.


			  Product Generation Table

   Note:  When entering the elevation slice parameter for a product which
   is to be generated for different weather modes, unless the product is to
   be generated for the exact elevation slices, separate entries for each
   weather mode should be made.  Furthermore, if the elevation slice
   parameter specifies a range of elevations(it is negative), then the same
   product can not be entered for a specific elevation slice unless the
   product for a different weather mode.


			Default_prod_gen table fields

   Parameter		Description			Possible Values
   =========================================================================
   prod_id		Product linear buffer number	Product buffer number
   wx_modes		Weather Mode Bits		0-6
			Bit 1 - precip mode
			Bit 2 - clear air mode
   gen_pr		Generate this product every	0-255
			gen_pr volume scans
   stor_pr		Store this product every	0-255
			stor_pr volume scans
   stor_retention	Storage retention time in
			number of minutes range of a
			4 byte integer
   p1 - p6		Product dependent parameters.	2 byte integer or
							UNU to indicate unused
							ALG to indicate this
							ALL to indicate all

SEE ALSO
      p_server(1), hci_prod(1), cs(3), mnttsk_pgt(1)















