mnttsk_pat(1)		RPG		mnttsk_pat(1)

NAME

   mnttsk_pd_info - Initialization Task for Product Attribute Table (PAT).

SYNOPSIS
   mnttsk_pat [-t mode] [-h]

DESCRIPTION

   This task performs initialization of the Product Attribute Table (PAT).
   Initialization activities depend on the startup mode/action (see Options).
   The activities are described here:

   clear:
   restart:
   startup:
   init:

      The initialization performed for startup modes "restart", "startup"
      "clear" and "init" are identical.  For all startup modes, the PAT 
      is initialized.  


   Options
	-t mode/action	startup mode/action.  Determines which initialization
			actions are to be performed.  Supported startup modes 
			or actions are "startup", "restart", "clear", and "init".
  			By default, the mode is "startup".  

	-h		Provides command line help.


SEE ALSO
	
      product_tables(4), ORPGDAT_PROD_INFO(4)

				$Revision: 1.2 $
