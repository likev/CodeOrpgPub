mda3d(4)		ORPG SUM Manage		mda3d(4)

NAME:
	mda3d Product

DESCRIPTION:
	The mda3d produces an intermedia product which is expected to
	feed mdattnn task. This product is generated at the end of each 
        elevation, and includes all 3D couplets (even those with 0 strength rank). 

	mda3d linear buffer stores data in the following format, which is from 
	c-structure new_cplt_t, mda_th_xs_t in the head file mda3d_acl.h.  

	typedef struct {
  	int   meso_id;
  	float base;
  	float top;
  	float depth;
  	float low_level_dia;
  	float max_level_dia;
  	float low_level_rot_vel;
  	float max_level_rot_vel;
  	float low_level_shear;
  	float max_level_shear;
  	float low_level_gtg_vel_diff;
  	float max_level_gtg_vel_diff;
  	int   cir_type;
  	float height_max_level_dia;
  	float height_max_level_rot_vel;
  	float height_max_level_shear;
  	float height_max_level_gtg_vel_diff;
  	float ll_center_azm;
  	float ll_center_rng;
  	float ll_center_x;
  	float ll_center_y;
  	float core_base;
  	float core_top;
  	float core_depth;
  	int ll_elev_sweep;
  	float msi;
  	int msi_rank;
  	float nssl_base;
  	float nssl_top;
  	float nssl_depth;
  	float storm_relative_depth;
  	int low_toped_meso;
  	float user_defined_base;
  	float user_defined_top;
  	float user_defined_depth;
  	int user_meso;
  	float center_azm;
  	float center_rng;
  	float low_level_convergence;
  	float mid_level_convergence;
  	int strength_rank;
  	float vert_integ_rot_vel;
  	float vert_integ_shear;
  	float vert_integ_gtg_vel_diff;
  	float delt_v_slope;
  	float trend_delt_v_slope;
  	int topped;
  	} new_cplt_t;

 	typedef struct {
  	int tilt_num;
  	int date;
  	int time;
  	int couplet_id;
  	int ht;
  	int dia;
  	int rot_vel;
  	int shr;
  	int gtgmax;
  	int rank;
  	} mda_th_xs_t;
	
	
        The product size is set to 500000 for worst case.
	The actual product size depends on the weather situation. 

PRODUCT ID: 
        MDA3D	

PRODUCT CODE:
  	294	

DEPENDANT PRODUCT:
        MDA2D(293)	

TYPE:
	volume Based

GENERATING TASK:
 	mda3d	

PATH:
	base/mda3d.lb

HDR:
	a309.h

SEE ALSO:
	product_tables(4), mda3d(1).

$Revision: 1.2 $
